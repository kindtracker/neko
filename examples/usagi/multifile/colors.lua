-- Tiny constants module — shows that `require`d files can simply return
-- a table of values, no functions needed.

return {
  PLAYER = gfx.COLOR_GREEN,
  ENEMY = gfx.COLOR_RED,
  PICKUP = gfx.COLOR_YELLOW,
}
